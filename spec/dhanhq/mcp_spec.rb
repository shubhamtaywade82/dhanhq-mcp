# frozen_string_literal: true

RSpec.describe Dhanhq::Mcp do
  it "has a version number" do
    expect(Dhanhq::Mcp::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
